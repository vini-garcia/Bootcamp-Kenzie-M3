import styled from "styled-components";

export const StyledCategoryOptionBySelect = styled.option`
  height: 2.5rem;
  font-size: 1rem;
  font-weight: 400;
  color: ${({ theme }) => theme.colors.greenPrimary};
`;
